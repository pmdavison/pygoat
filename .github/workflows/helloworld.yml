# Example Hello World workflow

name: HelloWorld

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  say-hello:
    name: Print Hello World
    runs-on: ubuntu-latest

    steps:

      - name: Say Hello
        run: echo "Hello, World!"
